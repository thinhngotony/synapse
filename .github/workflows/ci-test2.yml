name: CI
on:
  pull_request:
  push:
    branches: [master]
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
env:
  CARGO_TERM_COLOR: always
jobs:
  lint:
    name: clippy
    runs-on: ubuntu-22.04
    steps:
      - run: echo "CI with concurrency works"
